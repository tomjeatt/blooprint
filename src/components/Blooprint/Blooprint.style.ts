import styled, { css } from 'styled-components';

export const BlooprintWrapper = styled.div(() => {
  return css`
    background-color: transparent;
    display: flex;
  `;
});

export const Columns = styled.div(() => {
  return css`
    background-color: transparent;
    display: flex;
  `;
});
